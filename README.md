# DragON-Trial
 DragOn turns Windows login into a visual gesture game — drag icons across a themed image grid instead of typing a password. Built as a native Windows credential provider, it integrates seamlessly with   the Windows lock screen and supports both local and Microsoft accounts. Perfect for touchscreen devices where typing is impractical.
